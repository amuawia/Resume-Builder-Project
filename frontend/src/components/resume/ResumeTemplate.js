import React from 'react';

function ResumeTemplate({ template, data }) {
  return (
    <div className="resume-template">
      {/* Template rendering logic */}
    </div>
  );
}

export default ResumeTemplate;